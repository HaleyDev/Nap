# 计划：项目初始开发 — Metis Gastronomy Terminal

- **日期**：2026-04-23
- **状态**：已完成（优化计划部分进行中）
- **来源**：从 `plane.md` 迁移

---

## 1. 项目背景

开发者是一名资深嵌入式与后端工程师，追求极简、工业风及逻辑控制。

本项目旨在解决 **"周末吃什么"** 的决策焦虑，通过将二次元祈愿（Gacha）机制引入日常烹饪决策，建立一套**私有化、游戏化**的食谱管理与随机抽取系统。

该项目是 **Metis 个人数字助理生态**的一个模块，未来可能集成至 AI Agent 的工具链中。

---

## 2. 核心需求

| 需求项 | 说明 |
| --- | --- |
| **食谱抽奖** | 支持"祈愿 1 次"或"祈愿 10 次"，具备稀有度概率分布（3/4/5 星） |
| **工业风 UI** | 前端采用暗黑工业风，配合流星与卡片翻转动画，且必须兼容手机移动端 |
| **一体化部署** | 由 FastAPI 直接挂载静态资源，不使用 Nginx，追求部署结构最简化 |
| **私有库管理** | 提供独立的管理页面（Admin）用于录入和维护食谱数据 |

---

## 3. 技术栈

| 维度 | 技术选型 | 备注 |
| --- | --- | --- |
| 后端框架 | FastAPI (Python 3.11+) | 异步架构，负责 API 及静态文件托管 |
| 数据库 | PostgreSQL 13+ | Docker 容器化部署 |
| 持久层 | SQLAlchemy 2.0 (Async) | 使用 `asyncpg` 驱动 |
| 前端框架 | Vue 3 + TypeScript + Vuetify 3 | 已从 Vanilla JS 迁移（2026-04-23） |
| 构建工具 | Vite | 输出到 `app/templates/` |
| 数据交换 | Pydantic V2 | 强类型数据验证 |
| 容器化 | Docker 多阶段构建 | Node.js 编译前端 + Python 运行后端 |

---

## 4. 已完成的任务

### Phase 1：后端基础设施
- [x] 根据 `.env` 配置建立异步数据库连接
- [x] 实现 SQLAlchemy 模型与 `recipes` 表自动迁移
- [x] 编写 FastAPI 静态目录挂载代码

### Phase 2：核心 API 开发
- [x] 开发 `wish` 路由，实现概率抽取逻辑
- [x] 开发 `recipes` CRUD 接口
- [x] 开发图片上传接口
- [x] 开发批量导入接口
- [x] 开发推荐接口（每类随机一道）

### Phase 3：前端集成
- [x] 迁移到 Vue 3 + TypeScript + Vuetify 3
- [x] 实现抽奖页（WishView）- 配置表单、卡片翻转动画、详情弹窗
- [x] 实现管理页（AdminView）- CRUD 表单、图片上传、批量导入
- [x] Canvas 星空背景 + Web Audio 音效
- [x] LocalStorage 历史记录

### Phase 4：部署
- [x] Docker 多阶段构建（node:20-slim + python:3.11-slim）
- [x] docker-compose 编排（app + PostgreSQL）

---

## 5. 待优化任务

### A. 功能优化
- [x] 支持自定义灵感数量（1-20 滑块）
- [x] 支持根据关键字筛选
- [x] 菜谱支持 JSON 批量导入
- [x] 支持多次选择历史记录
- [x] 每个类别推荐一道（recommend 接口）
- [x] 修改菜谱字段：只保留食材和关键描述词，移除 steps 字段
- [ ] 去除固定稀有度百分比展示，改为可选配置

### B. UI 优化
- [x] 简化文字信息，减少冗余说明
- [x] 通用餐饮灵感表述（非仅限晚饭）
- [x] 进入页面直接进行抽奖配置
- [x] 抽奖结果为独立全屏页面
- [x] 添加抽奖音效
- [ ] 抽奖结果卡片显示范围加大
- [ ] 针对移动端深度优化
